Setting up yocto-autobuilder in four easy steps:
------------------------------------------------
git clone git://git.yoctoproject.org/yocto-autobuilder
cd yocto-autobuilder
. ./yocto-setup-autobuilder
yocto-start-autobuilder both

This sets up a minimal developer autobuilder with the following targets:

fuzzy-target
meta-target
nightly
eclipse-plugin
crownbay-noemgd
emenlow
n450
jasperforest
sugarbay

Please see README for: 
- more information on buildset targets 
- how to run the yocto-autobuilder as a build cluster across multiple machines
- example buildsets

